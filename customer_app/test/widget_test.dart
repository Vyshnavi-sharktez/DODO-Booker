import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('app smoke test', (WidgetTester tester) async {
    // Smoke tests will be added when screens are implemented.
    expect(true, isTrue);
  });
}
