import 'package:flutter/material.dart';

class AddressPlaceholder extends StatelessWidget {
  const AddressPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
